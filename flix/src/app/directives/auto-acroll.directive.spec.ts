import { AutoAcrollDirective } from './auto-scroll.directive';

describe('AutoAcrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAcrollDirective();
    expect(directive).toBeTruthy();
  });
});
